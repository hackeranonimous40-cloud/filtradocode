import { name, version } from '../../package.json'

export const MACRO = {
  VERSION: version,
  PACKAGE_URL: name,
  README_URL: 'https://github.com/opencarbo',
  ISSUES_EXPLAINER: 'use /bug command or file a GitHub issue at https://github.com/opencarbo/issues',
}
